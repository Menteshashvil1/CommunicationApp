services:
  postgres:
    image: postgres:16
    container_name: communications-postgres
    environment:
      POSTGRES_DB: communications_app
      POSTGRES_USER: communications_user
      POSTGRES_PASSWORD: communications_password
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
